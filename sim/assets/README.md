# web/assets

Static brand assets for the ViSiOMlab frontend (served at `/assets/...`).

Drop here:
- `lab-logo.svg` — the lab logo SVG to adapt into the ViSiOMlab wordmark/mark.
- `favicon.*` — the site favicon (svg/png/ico).
- Derived logo variants (header wordmark, square icon) once generated.
